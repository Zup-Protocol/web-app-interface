"use client";

/**
 * Standard keyboard event keys for reuse across the application.
 * Values follow the W3C UI Events KeyboardEvent key values.
 */
export enum KeyboardEventKey {
  Escape = "Escape",
  Enter = "Enter",
  Space = " ",
  ArrowUp = "ArrowUp",
  ArrowDown = "ArrowDown",
  ArrowLeft = "ArrowLeft",
  ArrowRight = "ArrowRight",
  Backspace = "Backspace",
  Delete = "Delete",
  Tab = "Tab",
}
